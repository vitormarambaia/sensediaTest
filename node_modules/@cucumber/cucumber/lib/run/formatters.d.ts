/// <reference types="node" />
import { IFormatterStream } from '../formatter';
import { EventEmitter } from 'events';
import { EventDataCollector } from '../formatter/helpers';
import { ISupportCodeLibrary } from '../support_code_library_builder/types';
import { IFormatterConfiguration } from '../configuration';
export declare function initializeFormatters({ cwd, stdout, eventBroadcaster, eventDataCollector, configuration, supportCodeLibrary, }: {
    cwd: string;
    stdout: IFormatterStream;
    eventBroadcaster: EventEmitter;
    eventDataCollector: EventDataCollector;
    configuration: IFormatterConfiguration;
    supportCodeLibrary: ISupportCodeLibrary;
}): Promise<() => Promise<void>>;
